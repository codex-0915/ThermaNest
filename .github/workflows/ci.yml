name: ThermaNest CI

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev, main]

jobs:
  build-firmware:
    name: Build Firmware
    runs-on: ubuntu-latest
    steps:
      - name: Stage Placeholder
        run: echo "Firmware CI will be added in future stages."

  lint-server:
    name: Lint Node.js Server
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v4

      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 18

      # - name: Install dependencies
      #   run: npm ci
      #   working-directory: server

      # - name: Run ESLint
      #   run: npx eslint .
      #   working-directory: server

      - name: Stage Placeholder
        run: echo "Lint Node.js Server CI will be added in future development."

  lint-dashboard:
    name: Lint Web Dashboard
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v4

      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 18

      # - name: Install dependencies
      #   run: npm ci
      #   working-directory: dashboard

      # - name: Run ESLint
      #   run: npx eslint .
      #   working-directory: dashboard

      - name: Stage Placeholder
        run: echo "Lint Web Dashboard CI will be added in future development."

